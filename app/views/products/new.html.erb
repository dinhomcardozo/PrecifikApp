<%= turbo_frame_tag "product_steps" do %>
  <%= render "steps",
        product:    @product,
        active_tab: (local_assigns[:active_tab] || params[:active_tab] || "config") %>

        <hr>
<% end %>