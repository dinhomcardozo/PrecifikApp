<p style="color: green"><%= notice %></p>

<% content_for :title, "Packages" %>

<%= link_to "[+]Criar pacote", new_package_path %>

<h1>Pacotes</h1>

<div id="packages">
  <% @packages.each do |package| %>
    <%= render package %>
    <p>
      <%= link_to "Ver", package %>
    </p>
  <% end %>
</div>