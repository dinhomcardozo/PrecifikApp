<% content_for :title, action_name == "new" ? "Novo equipamento" : "Editar equipamento" %>
<h1><%= yield :title %></h1>

<%= render "form", equipment: @equipment %>

<br>
<%= link_to "Voltar",
            services_equipments_path,
            class: "standart-button" %>